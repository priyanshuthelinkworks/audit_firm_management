import { Enum } from "#wms/Helper/utils";

const routes = {
  UploadController: [
    {
      name: "uploadFile", //unique name
      method: "post",
      route: "/upload",
      action: "uploadFile",
     },


      
]}

export default routes;